/**
 * Creates array with names from the aspects of each element in the array.
 * 
 * @param {Array.<String>}.
 * @param {<String>} 
 * @returns {Array.<String>} Common words.
 */
module.exports.fullName = function(names) {
  
  return 'Andrea Moulding';
};